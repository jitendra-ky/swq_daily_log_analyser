[project]
name = "backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.8",
    "ipykernel>=7.3.0",
    "jupyter>=1.1.1",
    "langchain-groq>=1.1.3",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
